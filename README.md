# Automation with Playwright & Typescript — Candidate README

## Setup
(Describe setup required to run the tests.)
Example format:
1. git clone <repo>
2. cd <folder>
3. npm install
4. npx playwright install
5. cp .env.example .env
6. # fill in .env values
7. npx playwright test

## External Assistance Disclosure
(List AI prompts used, selector tools, or any external code/library references. Provide short examples if possible.)

## Notes
(Any additional considerations, limitations, or special instructions.)

